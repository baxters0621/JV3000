# BITÁCORA DE CAMBIOS — JV3000 C.A.

Historial único de actividad del sistema. Cada entrada se registra AL TERMINAR una tarea y se commitea junto con el cambio (regla obligatoria en `AGENTS.md`).

## Formato de entrada

```
[YYYY-MM-DD HH:MM] · Módulo: <módulo> · Cambio: <qué se hizo y por qué> · Archivos: <ruta(s)> · Prueba: <verificación realizada>
```

---

## Registro

[2026-08-22 00:00] · Módulo: Dashboard · Cambio: Se corrige la tarjeta de ventas para mostrar la última venta registrada en lugar de filtrar solo por ventas del día actual; esto evita que el KPI quede en 0 cuando la nota más reciente no corresponde a hoy aunque exista en la tabla de últimas notas. · Archivos: `dashboard/index.php` · Prueba: `php -l dashboard/index.php`; consulta SQL confirmando la última venta activa en la base de prueba = $27.50 con fecha 2026-08-17.

[2026-08-13 15:36] · Módulo: Infraestructura MVC · Cambio: Respaldo pre-optimización — tag git `pre-optimizacion-v1` subido a GitHub y dump completo de la BD en backups/. · Archivos: `backups/jv3000_db_2026-08-13_153655.sql`, tag `pre-optimizacion-v1` · Prueba: dump verificado (20 tablas, 6 INSERTs, 30 KB); `git status` limpio.

[2026-08-13 15:38] · Módulo: Reglas del agente · Cambio: Se agrega la sección "Bitácora de cambios (OBLIGATORIA)" a AGENTS.md y se crea BITACORA.md como historial único de actividad del sistema. · Archivos: `AGENTS.md`, `BITACORA.md` · Prueba: regla documentada; formato de entrada definido.

[2026-08-13 15:40] · Módulo: Migración MVC · Cambio: Migración completa de módulos legacy a arquitectura MVC (Router/Controller/Model/Vista) — 11 controladores, 11 modelos, 13 vistas; front controller `index.php?url=...`. · Archivos: `core/*`, `controllers/*`, `models/*`, `views/*`, `index.php`, `config/config.php` · Prueba: `php -l` sin errores, smoke test HTTP de 10 rutas + POSTs + AJAX (commit `48e1e37`).

[2026-08-13 16:00] · Módulo: Dashboard · Cambio: Se reemplazan los enlaces legacy (dashboard/módulos/...) por rutas MVC en el panel y en el JS del dashboard, para que los accesos directos a productos/salidas/compras funcionen tras la migración. · Archivos: `dashboard/index.php`, `assets/dashboard/index.js` · Prueba: smoke test HTTP de rutas referenciadas (productos/salidas/compras) devuelven 200.

[2026-08-13 16:05] · Módulo: Legacy · Cambio: Los módulos legacy (módulos/_.php), su lógica de estadísticas y el AJAX de estadísticas se mueven a la carpeta `legacy/` como respaldo (decisión del usuario: mover y mantener). Se conservan activos los AJAX usados por el dashboard y el toolbox. · Archivos: `legacy/modules/_`, `legacy/includes/estadisticas_logic.php`, `legacy/includes/ajax/estadisticas_ajax.php`· Prueba:`php -l` de archivos movidos; smoke test HTTP de 11 rutas MVC = 200.

[2026-08-13 16:10] · Módulo: Helpers · Cambio: La función `jv_sello()` (sello ▲/▼ de comparación de porcentajes) se unifica: antes estaba duplicada en la vista de estadísticas y en el módulo legacy. Ahora vive en `includes/helpers.php` como helper global con docblock. · Archivos: `includes/helpers.php`, `views/estadisticas/index.php`, `legacy/modules/estadisticas.php` · Prueba: `php -l` sin errores; `index.php?url=estadisticas` devuelve 200.

[2026-08-13 16:20] · Módulo: JS compartido · Cambio: Se crea `assets/js/tooltips.js` con el tooltip global (mostrarTip/posicionarTip/ocultarTip) que antes estaba duplicado en 8 módulos JS; se eliminan las 8 copias locales y se carga el único archivo en el layout MVC y en la nota imprimible. También se unifican `limpiarErrores()`/`marcarError()` (duplicadas en 5 módulos) moviéndolas a `diseno.js`. · Archivos: `assets/js/tooltips.js`, `assets/js/diseno.js`, `assets/modules/*/*.js` (8 módulos), `includes/diseno.php`, `views/preview_factura/nota.php` · Prueba: `node --check` OK en los 10 JS; sin referencias huérfanas a los tooltips locales; smoke test HTTP = 200 y `?v=5` servido en todas las rutas.

[2026-08-13 16:40] · Módulo: Documentación del código · Cambio: Se añaden docblocks detallados en español a toda la capa PHP del proyecto (core/, controllers/, models/, includes/helpers.php) y comentarios de sección en `assets/js/diseno.js`, para que cada clase y método explique qué hace y por qué. Solo se insertaron comentarios; ninguna línea de lógica fue modificada. · Archivos: `core/*.php` (3), `controllers/*.php` (11), `models/*.php` (11), `includes/helpers.php`, `assets/js/diseno.js` · Prueba: `php -l` sin errores en los 26 archivos PHP; `node --check` OK en diseno.js; `git diff` confirma que solo se eliminaron líneas de comentario `//` (convertidas a docblocks); smoke test HTTP de 10 rutas = 200.

[2026-08-13 16:55] · Módulo: Documentación del proyecto · Cambio: Se actualiza README.md con la arquitectura MVC actual, la estructura de carpetas (core/controllers/models/views, legacy/, BITACORA.md) y la instalación mediante el auto-instalador `init.php` con `db/jv3000_portable_v4.sql`. · Archivos: `README.md` · Prueba: verificación de que `config/config.php`, `init.php` y `db/jv3000_portable_v4.sql` existen; referencias corregidas (antes apuntaban a `db/schema.sql` y `includes/config.php` inexistentes).

[2026-08-13 17:20] · Módulo: Legibilidad (módulo Categorías) · Cambio: Se simplifica la legibilidad del módulo Categorías SIN cambiar el funcionamiento: comentarios didácticos paso a paso en controlador, modelo, vista y JS; renombrado de variables crípticas (`$d`→`$datos`, `$dup`→`$duplicado`, `$cnt`→`$contador`, `$prox`→`$siguiente_numero`, `$idCat`→`$id_categoria`, `$abc`/`$tipo`→`$clasificacion_abc`/`$tipo_manejo`, `$row`→`$categoria_actual`, `$nuevo`→`$nuevo_estado`, `$n`→`$categoria`/`$sin_codigo`, `esDes`→`es_desactivar`). Cero cambios de lógica, SQL o valores. · Archivos: `controllers/CategoriasController.php`, `models/Categoria.php`, `views/categorias/index.php`, `assets/modules/categorias/categorias.js` · Prueba: `php -l` y `node --check` sin errores; smoke HTTP `?url=categorias` = 200; POST real de registrar creó la categoría con código `CAT-001` y POST toggle devolvió 302 (flujo completo OK); `git diff` confirma solo comentarios + renombrados; BD limpiada tras la prueba (0 categorías).

[2026-08-14 22:21] · Módulo: Legibilidad (módulo Categorías) · Cambio: Se eliminan las metáforas de los comentarios del módulo Categorías (`el "cocinero"`, `sello de seguridad`, `le dice al servidor`) reemplazándolas por texto literal, sin cambiar la lógica. · Archivos: `controllers/CategoriasController.php`, `views/categorias/index.php`, `assets/modules/categorias/categorias.js` · Prueba: `php -l` y `node --check` sin errores; smoke HTTP `?url=categorias` = 200.

[2026-08-16 23:57] · Módulo: Identidad del sistema · Cambio: Rebasado de marca completo para desvincular el sistema de la carga fiscal de "Facturación" y alinearlo al título oficial "Sistema Web para la Gestión de Inventario, Compras y Ventas en la Empresa JV3000, C.A ubicada en Valencia". (1) Lemas visibles actualizados: login, sidebar y dashboard ahora dicen "Gestión de Inventario, Compras y Ventas" (antes "Inventario y Facturación"); README actualizado (título, características y estructura). (2) Módulo interno `preview_factura` renombrado a `nota_entrega` (refleja que NO es factura fiscal sino Nota de Entrega): `controllers/PreviewFacturaController.php` → `NotaEntregaController.php`, `models/PreviewNota.php` → `NotaEntrega.php`, `views/preview_factura/` → `views/nota_entrega/`, `assets/modules/preview_factura/` → `assets/modules/nota_entrega/` (css/js renombrados a `nota_entrega.css/js?v=5`); rutas `?url=preview_factura` → `?url=nota_entrega` en SalidasController y salidas.js; referencias de CSS/JS y comentarios actualizados. (3) Carpeta `legacy/` (13 archivos de respaldo pre-MVC) eliminada con `git rm -r` — decisión del usuario; el código viejo sigue recuperable en el historial de git. NO se tocaron: la nota imprimible (IVA/Base Imponible/EXENTO), las validaciones RIF, la lógica ni las entradas históricas de esta bitácora. · Archivos: `login/login.php`, `includes/sidebar.php`, `dashboard/index.php`, `README.md`, `controllers/NotaEntregaController.php`, `models/NotaEntrega.php`, `views/nota_entrega/*`, `assets/modules/nota_entrega/*`, `controllers/SalidasController.php`, `assets/modules/salidas/salidas.js`, `assets/js/tooltips.js`, `legacy/*` (eliminado) · Prueba: `php -l` sin errores en los 8 PHP tocados; `node --check` OK en salidas.js, nota_entrega.js y tooltips.js; smoke HTTP con login real CSRF: `?url=salidas`, `?url=nota_entrega`, `?url=nota_entrega&token=zz`, `dashboard/index.php` y `?url=categorias` = 200; el título nuevo aparece en dashboard y sidebar (verificado en bytes UTF-8, "ó" correcto); AJAX `nota_entrega/store` responde JSON desde el controlador renombrado; **flujo real completo** (crear producto+lote de prueba → POST store → GET nota con token → POST salidas/confirm=302) guardó la salida con control `00-00000026` y detalle correctos, usando el CSS nuevo `nota_entrega.css?v=5`; grep final sin referencias activas a `preview_factura`/`PreviewNota`/`PreviewFactura` (solo la entrada histórica de esta bitácora); BD limpiada tras la prueba (0 salidas/productos/categorías/lotes/movimientos); `git status` muestra renames (R) del módulo y borrados (D) de legacy.

[2026-08-17 15:05] · Módulo: Datos demo + legibilidad + pulido · Cambio: (1) Carga de datos demo del rubro real de JV3000 C.A. (lubricantes automotrices): 8 categorías con códigos `CAT-001..008`, 4 proveedores con RIF válido `J-XXXXXXXX-X`, 16 productos con SKU `PROD-000001..016` (aceites de motor/motos/transmisión, líquido de frenos, refrigerantes, grasa, aditivos, filtros) y 3 clientes; contadores `CAT` y `PROD` sincronizados con los máximos usados (8 y 16). El stock inicial se cargó con lotes demo por producto (18 lotes totales) para que la venta FEFO por lote funcione correctamente (regla del modelo: si el producto tiene lotes, solo vende por lotes). (2) Ciclo en vivo completo por la app con login real CSRF: compra `FAC-001254` ($549.84, Pagada) → recepción (2 lotes, stock +50/+20, movimiento Entrada) → venta #6 (`00-00000029`, `NDE-000005`, FEFO descontó el lote 50→45, movimiento Salida). (3) Script de verificación de integridad: 0 filas en stock-vs-lotes, 0 contadores desincronizados, 0 FKs huérfanas, 0 duplicados de numeración. (4) Legibilidad de Ventas y Productos sin tocar lógica: renombradas variables crípticas de vistas (`$stk/$min/$max`→`$stock_actual/$stock_minimo/$capacidad`, `$stk_cls/$stk_lbl/$stk_pct/$bar_color`→`$stock_clase/$stock_etiqueta/$stock_porcentaje/$color_barra`, `$vc/$vi/$vd`→`$venc_badge/$venc_icono/$venc_fecha`, `$g`→`$grupo_mov`, `$tn/$obs/$causa`→`$nombre_tipo/$observaciones/$causa_ajuste`, `$tm`→`$tipo_mov`) con comentarios didácticos; modelos y controladores ya estaban legibles. (5) Pulido visual: verificación de estados vacíos y mensajes flash existentes en todas las vistas; sin restos visibles de "Facturación". · Archivos: `db/jv3000_portable_v4.sql`(no modificado; datos vía SQL directo),`views/productos/index.php`, `views/salidas/index.php`, `backups/jv3000_db_demo_2026-08-17_145258.sql`(respaldo) · Prueba:`php -l` sin errores en las 2 vistas; smoke HTTP con login real de 11 rutas = 200 sin fatales; dashboard AJAX devuelve venta $27.50 e inventario $3,362.90; nota imprimible de la venta #6 muestra NDE-000005 / 00-00000029 / JUAN PEREZ / V-11222333; verificación SQL de integridad en 0; ciclo compra→recepción→venta confirmado en BD con contadores avanzados consistentes (CTRL=29, NDE=5).

[2026-08-18 12:10] · Módulo: Legibilidad (Compras, Recepción y Proveedores) · Cambio: Se extiende la legibilidad (ya aplicada en Ventas/Productos) a las vistas de los 3 módulos, sin tocar lógica, SQL ni valores: `views/compras/index.php` (`$row`→`$compra`, `$p`→`$proveedor`, `$st`→`$status_pago` + comentario del estado de pago); `views/recepcion/index.php` (`$row`→`$compra_pendiente`, `$est`→`$estado_recepcion`, `$r`→`$recepcion` + comentario del estado); `views/proveedores/index.php` (`$row`→`$proveedor`, `$usado`→`$monto_credito_usado`, `$limite`→`$limite_credito`, `$disponible`→`$credito_disponible`, `$pct`→`$porcentaje_credito`, `$bar_color`→`$color_barra_credito` + comentario del bloque de crédito). Modelos y controladores ya estaban legibles. · Archivos: `views/compras/index.php`, `views/recepcion/index.php`, `views/proveedores/index.php` · Prueba: `php -l` sin errores en las 3 vistas; smoke HTTP con login real CSRF de `?url=compras`, `?url=recepcion` y `?url=proveedores` = 200 sin fatales y renderizando datos demo (FAC-001254, $549.84, Lubricentro Caribe, bloque de crédito); `git diff` confirma solo renombrados y comentarios (68 ins/65 del).

[2026-08-18 12:40] · Módulo: Seguridad (bloqueo de acceso directo) · Cambio: Se audita la exposición por HTTP del proyecto y se detecta una fuga crítica: los respaldos `backups/*.sql` (con hashes BCrypt de contraseñas de usuarios), el seed `db/jv3000_portable_v4.sql`, `backups/backup.bat` y el código fuente de `models/`, `controllers/`, `views/`, `config/`, `includes/` y `core/` se servían públicamente sin sesión (200), filtrando datos y rutas absolutas del servidor. Se crea `.htaccess` en la raíz con `mod_rewrite` que responde 403 a los 8 directorios internos (`backups|db|models|controllers|views|config|includes|core`) y a extensiones `.sql`/`.bat`/`.inc`. Se confirma que el "middleware" de sesión ya existía en `init.php` (valida sesión en todo salvo login/recuperar/logout → copiar una URL MVC en otro navegador sin sesión redirige al login 302). · Archivos: `.htaccess` (nuevo) · Prueba: HTTP real — 9 rutas internas (backups SQL, db seed, modelos, controladores, vistas, config, includes, core, backup.bat) = **403**; rutas públicas (index.php?url=compras/productos, dashboard, login, assets CSS/JS/SVG) = **200**; smoke con login real CSRF de 8 rutas MVC = 200 sin fatales; 14 assets CSS/JS cargados = 200.

[2026-08-18 12:55] · Módulo: Validación de formularios · Cambio: Auditoría de validación client-side: los formularios de datos (compras, salidas, productos, proveedores, categorías, recepción) ya validaban en JS y en servidor (Compra 13, Salida 20, Proveedor 19, Producto, Categoría 6 puntos); los formularios de estadísticas/historial son filtros de búsqueda GET (no requieren validación de datos). Único refuerzo necesario: atributos nativos `required` en los 4 campos numéricos obligatorios del formulario de edición de productos (stock mínimo, capacidad máx., precio venta, precio costo) que solo dependían del JS, añadiendo la capa nativa del navegador. · Archivos: `views/productos/index.php` · Prueba: `php -l` sin errores; smoke HTTP con login real de `?url=productos` = 200 sin fatales (se verifica en F4 con las 11 rutas).

[2026-08-18 13:10] · Módulo: Documentación JS de módulos · Cambio: Se documentan las 78 funciones de los 10 JS de módulos con comentarios didácticos en español (qué hace y por qué), para preparar la defensa del TEG estudiando el código a detalle. Solo se insertaron comentarios; cero cambios de lógica, strings ni valores. · Archivos: `assets/modules/{salidas,productos,compras,recepcion,categorias,proveedores,estadisticas,historial,nota_entrega,solicitudes_compra}/*.js` · Prueba: `node --check` sin errores en los 10 JS; `git diff` confirma 81 inserciones puras y ninguna línea de lógica modificada; smoke HTTP con login real de 11 rutas MVC + dashboard = 200 sin fatales; acceso sin sesión a `index.php?url=compras` y `dashboard/index.php` = 302 al login; bloqueo `.htaccess` vigente (9 rutas internas = 403).

[2026-08-21 00:00] · Módulo: Auditoría · Cambio: Se refuerza la trazabilidad del sistema agregando contexto operativo al registro de eventos: IP origen, ruta de la petición y método HTTP. Esto permite saber desde dónde y por qué se ejecutó cada acción crítica. La mejora es compatible con el resto del sistema y no rompe llamadas existentes porque la función acepta el mismo contrato anterior y agrega un parámetro opcional para metadatos. · Archivos: `includes/helpers.php`, `init.php`, `db/jv3000_portable_v4.sql` · Prueba: `php -l includes/helpers.php`; `php -l init.php`.

[2026-08-21 00:00] · Módulo: Seguridad · Cambio: Se corrige la lógica de bloqueo por IP en el login para que un mismo origen no genere filas duplicadas en `login_intentos`, y se normaliza el conteo de intentos con `ON DUPLICATE KEY UPDATE`. Además, se añade compatibilidad automática de esquema para la columna `ultimo_intento` y el índice único `idx_ip_unique` en `init.php`, evitando errores al levantar bases antiguas sin esos campos. Esto refuerza la seguridad del acceso sin romper el flujo actual de autenticación. · Archivos: `login/login.php`, `init.php`, `db/jv3000_portable_v4.sql` · Prueba: `php -l init.php`; `php -l login/login.php`; `php -l includes/helpers.php`.

[2026-08-21 00:05] · Módulo: Legibilidad del código · Cambio: Se renombran variables abreviadas de los flujos de registro, recuperación de contraseña y bloqueo del login por nombres que describen su contenido (`passwordStrength`, `securityAnswer`, `remainingSeconds`, entre otros). También se elimina una declaración duplicada de la respuesta de seguridad y se documenta en la guía la convención para nombres de PHP, JavaScript, clases, archivos y base de datos. No se modifican nombres de campos POST, rutas ni reglas de negocio. · Archivos: `assets/login/login.js`, `assets/login/recuperar.js`, `GUIA DE PROYECTO.md` · Prueba: `node --check assets/login/login.js`; `node --check assets/login/recuperar.js`.

[2026-08-21 00:10] · Módulo: Legibilidad de infraestructura · Cambio: Se renombran variables locales abreviadas de `init.php` relacionadas con la conexión sin base de datos, la comprobación del esquema, el token de instalación, el formulario de instalación y las migraciones. Los nombres de funciones, parámetros externos, rutas, campos SQL y comportamiento permanecen sin cambios. · Archivos: `init.php` · Prueba: `php -l init.php`; diagnóstico del editor sin errores.

[2026-08-21 00:15] · Módulo: Legibilidad de modelos · Cambio: Se renombran parámetros y variables abreviadas de los modelos `Proveedor` y `Producto` por nombres que describen los datos del formulario, estados y valores de inventario. No se cambian las claves de los arrays, nombres de métodos públicos, consultas ni reglas de validación. · Archivos: `models/Proveedor.php`, `models/Producto.php` · Prueba: `php -l models/Proveedor.php`; `php -l models/Producto.php`; diagnóstico del editor sin errores.

[2026-08-21 00:20] · Módulo: Legibilidad de estadísticas · Cambio: Se renombran variables genéricas del endpoint de estadísticas y de sus gráficos (`$d`, `d`, `fmt`, `cant`, `p` y similares) por nombres que explican si representan datos estadísticos, importes, cantidades, porcentajes o periodos. Se conservan sin cambios las claves del JSON y el comportamiento de los gráficos y filtros. · Archivos: `controllers/EstadisticasController.php`, `assets/modules/estadisticas/estadisticas.js` · Prueba: `php -l controllers/EstadisticasController.php`; `node --check assets/modules/estadisticas/estadisticas.js`.

[2026-08-21 00:25] · Módulo: Legibilidad de productos · Cambio: Se renombran las variables abreviadas de los filtros de productos (`st`, `vc`, `stk`, `min`, `btn`, `rows` y similares) por nombres que identifican estado, vencimiento, stock, mínimo y filas de productos. No se modifican atributos HTML, campos POST, filtros disponibles ni reglas del inventario. · Archivos: `assets/modules/productos/productos.js` · Prueba: `node --check assets/modules/productos/productos.js`; diagnóstico del editor sin errores.

[2026-08-21 00:30] · Módulo: Legibilidad de salidas · Cambio: Se renombran variables locales del flujo de ventas y solicitudes (`modalS`, `TIPO_MAP`, `s_productos`, `q`, `d`, `prod` y relacionadas) por nombres que explican el modal, los grupos de movimiento, los productos seleccionados y las respuestas de búsqueda. Se conservan las claves AJAX y los datos enviados al backend; además se repararon todas las referencias internas para evitar errores de ejecución. · Archivos: `assets/modules/salidas/salidas.js` · Prueba: `node --check assets/modules/salidas/salidas.js`; búsqueda de referencias antiguas sin resultados.

[2026-08-21 00:35] · Módulo: Legibilidad de recepción · Cambio: Se renombran variables locales de recepción (`datos`, `it`, `filter`, `rows` e índices genéricos) por nombres que identifican la compra, los productos pendientes, el texto de búsqueda y las filas de tabla. Se conservan los nombres de campos del formulario y los datos enviados al backend. · Archivos: `assets/modules/recepcion/recepcion.js` · Prueba: `node --check assets/modules/recepcion/recepcion.js`; diagnóstico del editor sin errores.

[2026-08-21 00:40] · Módulo: Corrección funcional · Cambio: Se corrige el endpoint de estadísticas para enviar importes numéricos sin separadores de miles, evitando que el auto-refresh convierta valores como `1,234.56` en `NaN`. También se corrige la expresión de validación visual de contraseñas en registro y recuperación para exigir realmente un símbolo, igual que el servidor. · Archivos: `controllers/EstadisticasController.php`, `assets/login/login.js`, `assets/login/recuperar.js` · Prueba: `php -l controllers/EstadisticasController.php`; `node --check assets/login/login.js`; `node --check assets/login/recuperar.js`.

[2026-08-21 00:45] · Módulo: Legibilidad de configuración · Cambio: Se renombra la clave `JV_CONFIG.c0` del login a `JV_CONFIG.remainingLockoutSeconds`, haciendo explícito que contiene los segundos restantes del bloqueo por intentos fallidos. Se actualizan tanto la vista PHP como el JavaScript consumidor, sin modificar el valor ni la lógica del contador. · Archivos: `login/login.php`, `assets/login/login.js` · Prueba: `php -l login/login.php`; `node --check assets/login/login.js`; búsqueda de `JV_CONFIG.c0` sin resultados en login.

[2026-08-21 00:50] · Módulo: Vistas y recursos frontend · Cambio: Se corrige el layout MVC para distinguir recursos locales de URLs absolutas. Antes se anteponía siempre `assets/` a los CSS y JavaScript externos, rompiendo las dependencias CDN usadas por proveedores. Ahora los recursos locales conservan la ruta de assets y los recursos `http://`, `https://` o protocol-relative mantienen su URL original; además se escapan sus atributos HTML. · Archivos: `views/layouts/main.php` · Prueba: `php -l views/layouts/main.php`; diagnóstico del editor sin errores.

[2026-08-21 00:55] · Módulo: Legibilidad de vistas · Cambio: Se renombran las variables genéricas `$r` de las vistas de reporte de inventario e historial por `producto` y `auditRecord`, indicando claramente qué registro se está renderizando. No se modifican los nombres de datos recibidos, cálculos, filtros ni HTML funcional. · Archivos: `views/reporte_inventario/index.php`, `views/historial/index.php` · Prueba: `php -l views/reporte_inventario/index.php`; `php -l views/historial/index.php`.

[2026-08-21 01:00] · Módulo: Seguridad y renderizado de vistas · Cambio: Se corrigen handlers inline de categorías, productos, proveedores y salidas para serializar valores dinámicos con `json_encode` y banderas `JSON_HEX_*`, evitando ruptura de JavaScript e inyección XSS por nombres almacenados con comillas o HTML. Se corrige el `colspan` del estado vacío de categorías de 5 a 6. En `NotaEntregaController` se añade el retorno después de renderizar una reimpresión para evitar concatenar la nota con la vista de error o preview. · Archivos: `views/categorias/index.php`, `views/productos/index.php`, `views/proveedores/index.php`, `views/salidas/index.php`, `controllers/NotaEntregaController.php` · Prueba: `php -l` de las vistas y controlador; revisión de serialización segura.

[2026-08-21 01:05] · Módulo: Contratos de vistas · Cambio: Se documentan mediante PHPDoc las variables que reciben las vistas de categorías, historial, nota de entrega y solicitudes desde sus controladores. Esto elimina advertencias estáticas causadas por `extract()` sin agregar valores ficticios ni ocultar errores de datos faltantes en ejecución. · Archivos: `views/categorias/index.php`, `views/historial/index.php`, `views/nota_entrega/nota.php`, `views/solicitudes/index.php` · Prueba: `php -l` de las cuatro vistas; revisión de diagnósticos del editor.

[2026-08-21 01:10] · Módulo: Contratos de vistas · Cambio: Se documentan mediante PHPDoc las variables recibidas por productos, proveedores, reporte de inventario y salidas. Con esto se completa el contrato estático de las vistas que usan `extract()` y se eliminan advertencias de variables indefinidas del editor sin modificar la lógica ni los datos enviados por los controladores. · Archivos: `views/productos/index.php`, `views/proveedores/index.php`, `views/reporte_inventario/index.php`, `views/salidas/index.php` · Prueba: `php -l` de las cuatro vistas; diagnóstico global de `views/`.

[2026-08-21 01:15] · Módulo: Legibilidad de vistas restantes · Cambio: Se renombran variables locales genéricas de compras y estadísticas (`$__base_url`, `$p` y `$clave`) por nombres que identifican la URL del listado, la clave del periodo y los datos del periodo. No se modifican los nombres de variables entregadas por los controladores ni el comportamiento de filtros y enlaces. · Archivos: `views/compras/index.php`, `views/estadisticas/index.php` · Prueba: `php -l` de ambas vistas; diagnóstico del editor sin errores.

[2026-08-21 01:20] · Módulo: Contratos de vistas · Cambio: Se documentan mediante PHPDoc las variables recibidas por compras, estadísticas y recepción. Con esto se elimina la última tanda de advertencias estáticas de `views/` provocadas por la extracción dinámica de datos desde los controladores, sin introducir valores por defecto ni alterar la lógica. · Archivos: `views/compras/index.php`, `views/estadisticas/index.php`, `views/recepcion/index.php` · Prueba: `php -l` de las tres vistas; diagnóstico global de `views/`.

[2026-08-21 01:25] · Módulo: Legibilidad de solicitudes · Cambio: Se renombran variables locales genéricas de la vista de solicitudes (`$row`, `$est`) por `pendingRequest`, `processedRequest` y `requestStatus`, diferenciando claramente solicitudes pendientes, solicitudes procesadas y su estado. No se cambian los datos recibidos, enlaces ni acciones del módulo. · Archivos: `views/solicitudes/index.php` · Prueba: `php -l views/solicitudes/index.php`; búsqueda de referencias antiguas sin resultados.

[2026-08-21 01:30] · Módulo: Legibilidad de categorías · Cambio: Se renombra la variable genérica `$row` de la tabla de categorías por `$category` en todas sus referencias, manteniendo sin cambios los campos, acciones y datos enviados a JavaScript. · Archivos: `views/categorias/index.php` · Prueba: `php -l views/categorias/index.php`; búsqueda de `$row` sin resultados.

[2026-08-21 01:35] · Módulo: Configuración frontend · Cambio: Se reemplazan las claves opacas de `JV_CONFIG` (`c0` y `c1`) por `csrfToken`, `taxPercentage` y `movementTypeGroups` en layout, controladores y JavaScript. Se elimina la configuración redundante de productos, que ya recibe el CSRF común del layout. Los nombres de campos POST y la lógica de negocio permanecen iguales. · Archivos: `views/layouts/main.php`, `controllers/ComprasController.php`, `controllers/ProductosController.php`, `controllers/SalidasController.php`, `assets/dashboard/usuarios.js`, `assets/modules/{categorias,compras,productos,proveedores,salidas,solicitudes_compra}/*.js` · Prueba: `php -l` de controladores/layout; `node --check` de consumidores.

[2026-08-21 01:40] · Módulo: Documentación de configuración · Cambio: Se actualiza el comentario del modelo de salidas para reflejar `JV_CONFIG.movementTypeGroups` y evitar referencias documentales al nombre eliminado `c0`. · Archivos: `models/Salida.php` · Prueba: búsqueda global de `JV_CONFIG.c0` sin resultados funcionales; `php -l models/Salida.php`.

[2026-08-21 01:45] · Módulo: Legibilidad de backend · Cambio: Se renombran variables locales abreviadas en `ProductosController` y `Salida` para identificar el producto a cambiar de estado, el resultado de la operación, el tipo de movimiento y el mapa de grupos. Se mantienen sin cambios los nombres POST, métodos públicos, consultas y reglas de negocio. · Archivos: `controllers/ProductosController.php`, `models/Salida.php` · Prueba: `php -l controllers/ProductosController.php`; `php -l models/Salida.php`; búsqueda de referencias anteriores sin resultados.

[2026-08-21 01:50] · Módulo: Legibilidad de recepción · Cambio: Se renombra el parámetro genérico `$post` de `Recepcion::registrar()` por `$receptionFormData` y se actualizan sus usos directos, manteniendo intactas las claves del formulario, las consultas y las reglas de recepción. · Archivos: `models/Recepcion.php` · Prueba: `php -l models/Recepcion.php`; búsqueda de `$post` sin resultados.

[2026-08-21 01:55] · Módulo: Legibilidad de compras · Cambio: Se renombran variables genéricas de `Compra` relacionadas con filas de crédito, solicitudes, detalles y datos del formulario (`$rows`, `$r`, `$sol`, `$det`, `$d`, `$post`) por nombres descriptivos. Se conservan las claves del formulario, consultas, métodos públicos y reglas de compra. · Archivos: `models/Compra.php` · Prueba: `php -l models/Compra.php`; búsqueda de referencias anteriores sin resultados.

[2026-08-21 02:00] · Módulo: Documentación del proyecto · Cambio: Se actualiza `GUIA DE PROYECTO.md` con una tabla de equivalencias de nombres reales aplicados durante la mejora de legibilidad, explicando el significado de cada identificador y aclarando qué nombres de contratos externos deben conservarse. · Archivos: `GUIA DE PROYECTO.md` · Prueba: revisión de equivalencias contra los archivos modificados.

[2026-08-21 02:05] · Módulo: Legibilidad de solicitudes · Cambio: Se renombran variables locales abreviadas del modelo de solicitudes (`$it`, `$dups`, `$p`, `$d`, `$mapa`, `$idProd`, `$cant`) por nombres que identifican ítems, duplicados, productos, mapa de nombres y cantidades. Se conservan las claves SQL, los resultados públicos y las reglas de validación. · Archivos: `models/Solicitud.php` · Prueba: `php -l models/Solicitud.php`; búsqueda de referencias anteriores sin resultados.

[2026-08-21 02:10] · Módulo: Legibilidad de controladores · Cambio: Se renombran variables locales genéricas de categorías y proveedores (`$datos`, `$accion`, `$map`, `$flash_s`) por nombres que identifican datos de formulario, acciones, mensajes y flash de sesión. Se conservan las claves POST, rutas, respuestas y datos entregados a las vistas. · Archivos: `controllers/CategoriasController.php`, `controllers/ProveedoresController.php` · Prueba: `php -l` de ambos controladores; diagnóstico del editor sin errores.

[2026-08-21 02:15] · Módulo: Legibilidad de solicitudes · Cambio: Se renombran variables locales de `SolicitudesController` (`$itemsRaw`, `$motivo`, `$idUsuario`, `$resultado`, `$id`) por nombres que identifican ítems crudos, motivo, usuario actual, resultado de operación e identificador de solicitud. Se mantienen intactos los campos POST, respuestas JSON, rutas y permisos. · Archivos: `controllers/SolicitudesController.php` · Prueba: `php -l controllers/SolicitudesController.php`; búsqueda de referencias anteriores sin resultados.

[2026-08-21 02:20] · Módulo: Legibilidad de vistas · Cambio: Se renombran variables locales genéricas de historial y nota de entrega (`$a`, `$i`, `$av`, `$det`, `$fila_total`) por nombres que identifican acciones, páginas, alertas, detalles y totales de línea. No se modifican variables recibidas, campos de datos ni cálculos. · Archivos: `views/historial/index.php`, `views/nota_entrega/nota.php` · Prueba: `php -l` de ambas vistas; diagnóstico del editor sin errores.

[2026-08-21 02:25] · Módulo: Legibilidad de salidas · Cambio: Se renombra `$row` a `$outgoingRecord` y `$m` a `$causeMatch` en la tabla de salidas, haciendo explícitos el registro renderizado y el resultado de la causa del movimiento. Se mantienen intactos los campos, acciones, cálculos y datos enviados a JavaScript. · Archivos: `views/salidas/index.php` · Prueba: `php -l views/salidas/index.php`; búsqueda de referencias antiguas sin resultados.

[2026-08-21 02:30] · Módulo: Legibilidad de productos · Cambio: Se completa el renombrado de `$row` a `$productRecord` y de `$venc` a `$expirationDate` en el bloque de inventario, incluyendo estado, precios, vencimiento y acciones administrativas. No se modifican campos HTML, cálculos de stock, filtros ni acciones JavaScript. · Archivos: `views/productos/index.php` · Prueba: `php -l views/productos/index.php`; diagnóstico del editor sin errores; búsqueda de referencias antiguas sin resultados.

[2026-08-21 02:35] · Módulo: Legibilidad de paginación · Cambio: Se renombra el índice genérico `$i` de la paginación de productos por `$pageNumber`, haciendo explícito que representa el número de página. No se modifica la navegación ni los parámetros de URL. · Archivos: `views/productos/index.php` · Prueba: `php -l views/productos/index.php`; búsqueda global de variables de iteración genéricas sin resultados en las vistas.

[2026-08-21 02:40] · Módulo: Codificación de vistas · Cambio: Se corrigen textos visibles y comentarios con codificación dañada en la vista de productos (`CÃ³DIGO`, `mÃ­nimo`, `CategorÃ­a`, `â€”`, entre otros), restaurándolos a UTF-8 legible. No se modifican lógica, datos ni atributos funcionales. · Archivos: `views/productos/index.php` · Prueba: `php -l views/productos/index.php`; búsqueda de patrones de codificación dañada en la vista.

[2026-08-21 02:45] · Módulo: Codificación de documentación PHP · Cambio: Se corrigen los caracteres dañados de los comentarios de `NotaEntregaController`, incluyendo flechas, acentos y referencias a sesión, método y reimpresión. No se modifica la lógica del controlador. · Archivos: `controllers/NotaEntregaController.php` · Prueba: `php -l controllers/NotaEntregaController.php`; búsqueda de patrones de codificación dañada.

[2026-08-21 02:50] · Módulo: Legibilidad de compras · Cambio: Se renombra la función local `fmt` a `formatCurrency` y su parámetro `n` a `amount`, haciendo explícito que formatea importes monetarios. Se actualizan todas sus llamadas sin cambiar cálculos ni valores mostrados. · Archivos: `assets/modules/compras/compras.js` · Prueba: `node --check assets/modules/compras/compras.js`; búsqueda de `fmt` sin resultados.

[2026-08-21 02:55] · Módulo: Legibilidad de compras · Cambio: Se completan los nombres descriptivos del JavaScript de compras: búsqueda (`searchTerm`, `searchResponse`), selección (`productElement`), cantidades (`requestedQuantity`), vencimiento (`expirationDate`), filas (`product`, `productIndex`) y precarga (`requestItem`). Se conservan las claves AJAX y del formulario. · Archivos: `assets/modules/compras/compras.js` · Prueba: `node --check assets/modules/compras/compras.js`; búsqueda de abreviaturas antiguas sin resultados.

[2026-08-21 03:00] · Módulo: Legibilidad de salidas · Cambio: Se completan los nombres descriptivos de búsqueda de clientes, documentos, cantidades y eliminación de productos en `salidas.js` (`searchTerm`, `clientElement`, `clientDocument`, `documentMatch`, `requestedQuantity`, `availableStock`, `productIndex`). Se mantienen las claves AJAX, propiedades de producto y datos del formulario. · Archivos: `assets/modules/salidas/salidas.js` · Prueba: `node --check assets/modules/salidas/salidas.js`; búsqueda de abreviaturas antiguas sin resultados.

[2026-08-21 03:05] · Módulo: Legibilidad frontend · Cambio: Se renombran variables locales de proveedores y recepción para identificar entradas monetarias, elementos de proveedor, cantidades recibidas, cantidades pendientes, filas y fechas de vencimiento. Se conservan los nombres de campos, atributos `data-*` y contratos del formulario. · Archivos: `assets/modules/proveedores/proveedores.js`, `assets/modules/recepcion/recepcion.js` · Prueba: `node --check` de ambos archivos; diagnóstico del editor sin errores.

[2026-08-21 03:10] · Módulo: Legibilidad del dashboard · Cambio: Se renombran variables locales abreviadas de tooltips y posicionamiento de la campana de alertas (`r`, `el`, `btn`, `pw`, `ph`) por nombres que identifican límites, elementos y dimensiones visuales. No se modifican selectores, eventos ni comportamiento del dashboard. · Archivos: `assets/dashboard/index.js` · Prueba: `node --check assets/dashboard/index.js`; diagnóstico del editor sin errores.

[2026-08-21 03:15] · Módulo: Legibilidad del dashboard · Cambio: Se renombra la última variable `btn` del cierre automático del panel de alertas por `alertButton`, completando las referencias del archivo. · Archivos: `assets/dashboard/index.js` · Prueba: `node --check assets/dashboard/index.js`; búsqueda de variables abreviadas sin resultados.

[2026-08-21 03:20] · Módulo: Organización documental · Cambio: Se crea la carpeta `docs/` y se trasladan `README.md`, `GUIA DE PROYECTO.md` y `BITACORA.md` para mantener la raíz del proyecto ordenada. Se actualizan las referencias de `AGENTS.md`, README y la guía; `AGENTS.md` permanece en la raíz por ser el archivo de instrucciones del agente. · Archivos: `docs/README.md`, `docs/GUIA DE PROYECTO.md`, `docs/BITACORA.md`, `AGENTS.md` · Prueba: archivos existentes en `docs/`; búsqueda de referencias actualizadas.

[2026-08-21 03:25] · Módulo: Legibilidad de nota de entrega · Cambio: Se renombran variables locales del controlador de nota de entrega (`$id`, `$data`, `$detalles`, `$tn`, `$datos`) por nombres que identifican la salida, el preview, sus detalles, el tipo de movimiento y los datos de la nota. También se corrigen comentarios con codificación dañada. · Archivos: `controllers/NotaEntregaController.php`, `docs/GUIA DE PROYECTO.md` · Prueba: `php -l controllers/NotaEntregaController.php`; búsqueda de referencias antiguas sin resultados.

[2026-08-21 03:30] · Módulo: Validación y codificación · Cambio: Se completa la corrección de los comentarios restantes con caracteres dañados en `NotaEntregaController` y se confirma que no quedan patrones de codificación incorrecta en controladores, vistas ni documentación. · Archivos: `controllers/NotaEntregaController.php`, `docs/GUIA DE PROYECTO.md` · Prueba: `php -l` global de PHP; `node --check` global de JavaScript no minificado; búsqueda sin resultados.

[2026-08-21 03:35] · Módulo: Documentación y enlaces · Cambio: Se normaliza completamente `docs/GUIA DE PROYECTO.md` a UTF-8 legible y se corrigen sus enlaces relativos tras el traslado a `docs/`, evitando mojibake y rutas Markdown inválidas. · Archivos: `docs/GUIA DE PROYECTO.md` · Prueba: búsqueda sin resultados de patrones `CÃ`/`â`; revisión de enlaces sin paréntesis sobrantes; diagnóstico del editor sin errores.

[2026-08-21 03:40] · Módulo: Pruebas funcionales · Cambio: Se ejecuta smoke test real con Apache y MySQL activos usando sesión autenticada temporal. El login redirige al dashboard; las 11 rutas MVC (`categorias`, `compras`, `estadisticas`, `historial`, `nota_entrega`, `productos`, `proveedores`, `recepcion`, `reporte_inventario`, `salidas`, `solicitudes`) responden HTTP 200; el endpoint `estadisticas/datos` responde HTTP 200 con JSON exitoso; proveedores conserva sus recursos CDN. · Archivos: aplicación completa · Prueba: curl con cookie temporal; todas las rutas = 200; estadísticas AJAX `success=true`; recursos CDN detectados.

[2026-08-21 03:45] · Módulo: Legibilidad de autenticación y salidas · Cambio: Se renombran variables locales de confirmación/anulación de salidas y recuperación de contraseña (`$previewToken`, `$previewData`, `$confirmationResult`, `$recoveryInput`, `$userAccount`, `$securityAnswer`, `$newPassword`, entre otras). Se elimina una variable de recuperación no utilizada y se mantienen intactos los campos POST, claves de sesión, rutas y reglas de seguridad. · Archivos: `controllers/SalidasController.php`, `login/recuperar.php` · Prueba: `php -l` de ambos archivos; diagnóstico del editor sin errores.

[2026-08-21 03:50] · Módulo: Documentación de nombres · Cambio: Se actualiza la guía con las equivalencias de los últimos renombrados en utilidades, compras, salidas, dashboard y autenticación, explicando el significado de cada identificador y manteniendo separados los contratos externos de los nombres internos. · Archivos: `docs/GUIA DE PROYECTO.md` · Prueba: revisión de equivalencias contra el código actual; diagnóstico del editor sin errores.

[2026-08-21 04:00] · Módulo: Continuidad del proyecto · Cambio: Se añade a la guía la lista de tareas reservadas para la siguiente sesión: pruebas automatizadas y funcionales con base de datos de prueba, revisión de accesibilidad/responsive y preparación para despliegue. · Archivos: `docs/GUIA DE PROYECTO.md` · Prueba: sección de continuidad visible al final de la guía.

[2026-08-21 04:05] · Módulo: Continuidad del proyecto · Cambio: Se crea un documento independiente con todas las tareas pendientes, su orden recomendado, validaciones necesarias y la regla de proteger la base de datos real antes de ejecutar operaciones de escritura. Se enlaza desde la guía principal. · Archivos: `docs/PLAN DE CONTINUACION.md`, `docs/GUIA DE PROYECTO.md` · Prueba: documento creado y enlace verificado.

[2026-08-21 00:00] · Módulo: Login y migración de esquema · Cambio: Se define explícitamente la ruta de assets usada por la vista de login para eliminar la referencia indefinida detectada por el editor. También se eliminan duplicados históricos por IP antes de crear el índice único de `login_intentos`, evitando que la migración automática falle en instalaciones antiguas. · Archivos: `login/login.php`, `init.php` · Prueba: `php -l login/login.php`; `php -l init.php`; comprobación del diagnóstico de `$base_assets`.

[2026-08-22 09:15] · Módulo: Continuidad de pruebas · Cambio: Se protege la base de producción con respaldo completo y se crea una base de prueba aislada `jv3000_db_test` para continuar con validaciones funcionales sin tocar datos operativos. Se deja el punto de partida documentado en la guía de continuación y la bitácora para seguir con la batería de pruebas del flujo de usuarios, compras, recepción, ventas y seguridad. · Archivos: `docs/PLAN DE CONTINUACION.md`, `docs/BITACORA.md`, `backups/jv3000_db_2026-08-22_091425.sql` · Prueba: `Get-Service mysql` mostró el servicio `mysql` en estado Running; `SHOW DATABASES` confirmó la existencia de `jv3000_db` y `jv3000_db_test`; la base de prueba importó la estructura y tablas principales correctamente.

[2026-08-22 09:30] · Módulo: Continuidad de pruebas · Cambio: Se validan en la interfaz autenticada y exclusivamente sobre `jv3000_db_test` los flujos de creación de categoría, creación de proveedor y edición de producto. La categoría recibe el código `CAT-010`; el proveedor conserva RIF, teléfono E.164, correo y crédito; el producto actualiza precios sin alterar su stock. · Archivos: `docs/BITACORA.md` · Prueba: consultas SQL en `jv3000_db_test` confirmaron categoría `CATEGORIA QA VALIDACION`, proveedor `PROVEEDOR QA VALIDACION` y producto `PROD-000003` con stock `40`, precio venta `6.25` y costo `4.45`; no se escribieron datos en `jv3000_db`.
[2026-08-22 11:06] · Módulo: Continuidad de pruebas · Cambio: Se valida el flujo de compra y recepción en `jv3000_db_test`: la factura `QA-000001` queda completa, incrementa el stock del producto `PROD-000003`, crea su lote con vencimiento y registra el movimiento de entrada con guía/recibo. · Archivos: `docs/BITACORA.md` · Prueba: la interfaz autenticada confirmó la recepción de 5 unidades; SQL confirmó stock `40 → 45`, lote `23` con 5 unidades restantes y movimiento `Entrada` activo con documento `GUIA-QA-000001`; no se escribieron datos en `jv3000_db`.
[2026-08-22 11:20] · Módulo: Interfaz de ventas y navegación · Cambio: Se corrige el backdrop del sidebar para que solo sea interactivo en móvil, evitando que bloquee botones y modales en escritorio; se versionan los recursos para invalidar caché. En ventas, el cliente escrito manualmente sincroniza inmediatamente el campo oculto requerido por el backend. · Archivos: `assets/js/sidebar.js`, `assets/modules/salidas/salidas.js`, `includes/sidebar.php`, `controllers/SalidasController.php`, `docs/BITACORA.md` · Prueba: `php -l controllers/SalidasController.php`; `node --check assets/js/sidebar.js`; `node --check assets/modules/salidas/salidas.js`; revisión de `sidebar.js?v=4` y `salidas.js?v=6`; la operación de venta incompleta no alteró stock ni creó registros.
[2026-08-22 11:35] · Módulo: Codificación de interfaz · Cambio: Se corrigen etiquetas visibles dañadas en Inventario (`CATEGORÍA` y `STOCK MÍNIMO`) y se repara en la base aislada el nombre del tipo de movimiento `Daños`, que aparecía como `Da??os` por datos corruptos. · Archivos: `views/productos/index.php`, `jv3000_db_test.tipos_movimientos`, `docs/BITACORA.md` · Prueba: `php -l views/productos/index.php`; navegador confirmó `CATEGORÍA`, ausencia de patrones `CATEGORÃ`/`MÃNIMO` y opción `Daños`; bytes almacenados del nombre reparado: `4461C3B16F73`; no se modificó la base real.
[2026-08-22 11:50] · Módulo: Interfaz responsive de proveedores · Cambio: Se corrige el desbordamiento horizontal del encabezado de Proveedores en móvil reorganizando buscador, filtros y acción en filas adaptables; se versiona el CSS para invalidar caché. · Archivos: `views/proveedores/index.php`, `assets/modules/proveedores/proveedores.css`, `controllers/ProveedoresController.php`, `docs/BITACORA.md` · Prueba: `php -l views/proveedores/index.php`; auditoría móvil confirmó `scrollWidth=661px` antes y `scrollWidth=375px` después, sin overflow; el navegador cargó `proveedores.css?v=3` y las demás rutas no presentaron overflow.
[2026-08-22 12:10] · Módulo: Continuidad de pruebas · Cambio: Se intenta avanzar con el preview de una venta FEFO usando `PROD-000003`, cliente existente y 2 unidades en `jv3000_db_test`; la interfaz genera el preview y cierra el modal, pero el navegador integrado no expone la pestaña nueva de la Nota de Entrega para confirmar. · Archivos: `docs/BITACORA.md` · Prueba: SQL confirmó que no se creó una venta adicional, el stock permanece en `45` y el lote `23` conserva `5` unidades; no se alteró la base real.
[2026-08-22 12:30] · Módulo: Flujo de Nota de Entrega · Cambio: Se corrige el avance del preview de ventas para navegar en la misma pestaña hacia la Nota de Entrega, evitando pérdida del contexto de sesión o bloqueo de ventanas emergentes al confirmar una venta. Se versiona `salidas.js` para invalidar caché; la reimpresión existente conserva su apertura independiente. · Archivos: `assets/modules/salidas/salidas.js`, `controllers/SalidasController.php`, `docs/BITACORA.md` · Prueba: `node --check assets/modules/salidas/salidas.js`; prueba previa confirmó que el fallo ocurría al depender de la pestaña nueva sin afectar stock; validación funcional de confirmación pendiente.
[2026-08-22 12:45] · Módulo: Continuidad de pruebas · Cambio: Se completa en `jv3000_db_test` el flujo de venta FEFO y anulación: se confirma la nota `NDE-000006` por 2 unidades de `PROD-000003`, se consume el lote vigente más antiguo y luego se anula la salida para restaurar inventario. · Archivos: `assets/modules/salidas/salidas.js`, `controllers/SalidasController.php`, `docs/BITACORA.md` · Prueba: la Nota de Entrega se abrió en la misma pestaña con token; SQL confirmó consumo del lote `9` (`40 → 38`), stock del producto (`45 → 43`) y posterior restauración a lote `40` y stock `45`; salida quedó `Anulada` y movimiento de venta `Anulado`; no se modificó la base real.
[2026-08-22 13:05] · Módulo: Continuidad de pruebas · Cambio: Se valida el flujo de solicitud de reposición: desde Ventas se solicita una cantidad superior al stock, la solicitud queda pendiente, Compras la precarga y al registrar la factura la marca como `Atendida`. · Archivos: `docs/BITACORA.md` · Prueba: la solicitud `#4` pidió 50 unidades de `PROD-000003`; el modal de Compras precargó producto y cantidad, la factura `QA-REQ-0001` quedó vinculada y pendiente de recepción; stock permaneció en `45`, sin modificar la base real.
[2026-08-22 13:20] · Módulo: Continuidad de pruebas · Cambio: Se valida la reimpresión de una Nota de Entrega y el filtro de pagos de Compras; también se comprueba que Estadísticas cargue sus KPIs y que su endpoint rechace accesos directos que no sean AJAX. · Archivos: `docs/BITACORA.md` · Prueba: `nota_entrega&id=6` mostró `NDE-000005`, detalle y botón de impresión; `compras&filtro_pago=Pendiente` mostró solo las facturas pendientes; `estadisticas` cargó ventas/compras/ganancia y `estadisticas/datos` sin cabecera AJAX respondió `acceso_denegado`; no hubo escrituras adicionales.
[2026-08-22 13:35] · Módulo: Seguridad HTTP · Cambio: Se detecta y corrige la ausencia de `.htaccess` en la raíz, que permitía servir por HTTP código fuente, respaldos SQL y archivos de configuración. Se añaden reglas de Apache para bloquear directorios internos y extensiones sensibles. · Archivos: `.htaccess`, `docs/BITACORA.md` · Prueba: auditoría inicial devolvía `HTTP 200` en 9 rutas internas; verificación posterior confirmó `HTTP 403` en respaldos, SQL, código y configuración; las rutas MVC sin sesión continuaron redirigiendo `302` al login.
[2026-08-22 16:10] · Módulo: Auditoría de seguridad · Cambio: Se verifica el comportamiento previo al controlador para peticiones AJAX sin sesión y se revisan las cabeceras de seguridad emitidas por Apache/PHP. · Archivos: `docs/BITACORA.md` · Prueba: POST con CSRF ausente y alterado, sin sesión, devolvió `302` al login en ambos casos; las respuestas incluyeron `HttpOnly`, `SameSite=Strict`, CSP, `X-Frame-Options: SAMEORIGIN`, `X-Content-Type-Options: nosniff`, `Referrer-Policy` y `Permissions-Policy`; prueba CSRF con sesión autenticada queda pendiente.
[2026-08-22 16:25] · Módulo: Auditoría CSRF · Cambio: Se valida con sesión autenticada que un token CSRF alterado bloquea un POST de creación antes de ejecutar la lógica del módulo. · Archivos: `docs/BITACORA.md` · Prueba: el formulario de Categorías se envió con token `token-invalido`, no creó `CATEGORIA CSRF INVALIDA` (`0` filas en `jv3000_db_test`) y permaneció en el listado; la base real no fue modificada.
[2026-08-22 16:40] · Módulo: Auditoría de autenticación · Cambio: Se valida el bloqueo por IP después de tres intentos fallidos de inicio de sesión. · Archivos: `docs/BITACORA.md` · Prueba: la pantalla mostró `ACCESO BLOQUEADO POR 2 MINUTOS (3 intentos fallidos)`, deshabilitó usuario, clave y botón de autenticación, y mostró contador de 120 segundos; prueba realizada contra `jv3000_db_test`.
[2026-08-22 16:50] · Módulo: Automatización de validación · Cambio: Se ejecuta smoke test de sintaxis sobre el código actual para detectar errores antes de continuar con las pruebas visuales y funcionales. · Archivos: `docs/BITACORA.md` · Prueba: `php -l` sin fallos en 57 archivos PHP y `node --check` sin fallos en 18 archivos JavaScript propios no minificados; la prueba de paginación quedó temporalmente bloqueada por el contador de seguridad de la IP de prueba.
[2026-08-22 17:05] · Módulo: Continuidad de pruebas · Cambio: Se valida la navegación paginada del Inventario y el filtro mensual de Estadísticas sin operaciones de escritura. · Archivos: `docs/BITACORA.md` · Prueba: Inventario con `p=2` mostró estado vacío coherente porque los registros caben en la primera página de 30; Estadísticas con `periodo=mes` mostró comparativo mensual, KPIs y 2 gráficos activos; no se modificaron datos.
[2026-08-22 17:20] · Módulo: Recursos frontend · Cambio: Se investiga el aviso transitorio `HTTP 403` detectado previamente en consola durante la carga de Estadísticas. · Archivos: `docs/BITACORA.md` · Prueba: recarga autenticada con captura de respuestas no reprodujo errores `4xx/5xx`; Estadísticas cargó correctamente sus KPIs, periodo mensual y 2 gráficos; no fue necesario modificar código.
[2026-08-22 17:35] · Módulo: Usuarios · Cambio: Se corrige un error JavaScript del panel de Colaboradores: el observer del sidebar podía ejecutar `classList` sobre `mainWrapper` nulo porque el script se carga antes del contenedor. Se añade una guarda y se versiona el recurso. · Archivos: `assets/dashboard/usuarios.js`, `dashboard/usuarios.php`, `docs/BITACORA.md` · Prueba: error reproducido como `TypeError ... classList` en `usuarios.js?v=3`; recarga posterior con `usuarios.js?v=4` no registró errores de consola; la cuenta QA permaneció sin cambios.
[2026-08-22 18:20] · Módulo: Dashboard · Cambio: Se corrige el `403` del AJAX de alertas causado por el bloqueo global de `includes/` en `.htaccess`, permitiendo únicamente `includes/ajax/alertas_ajax.php`, que mantiene su validación de sesión y cabecera AJAX. También se normaliza `JV_CONFIG` del panel de usuarios para exponer `csrfToken`, clave consumida por las acciones administrativas. · Archivos: `.htaccess`, `dashboard/usuarios.php`, `docs/BITACORA.md` · Prueba: error reproducido en consola al cargar el dashboard; validación posterior confirmó endpoint AJAX autenticado `HTTP 200` con JSON válido, recarga del dashboard sin respuestas `4xx/5xx` ni errores de consola, y `includes/helpers.php` permanece en `HTTP 403`.
[2026-08-22 18:35] · Módulo: Dashboard · Cambio: Se confirma la corrección en una sesión autenticada nueva y se descarta como causa del Dashboard el HTML de login observado en pestañas expiradas. · Archivos: `.htaccess`, `dashboard/index.php`, `dashboard/usuarios.php`, `assets/dashboard/index.js`, `assets/dashboard/usuarios.js`, `docs/BITACORA.md` · Prueba: recarga limpia sin errores de consola ni respuestas `4xx/5xx`; `alertas_ajax.php` autenticado respondió `200` con JSON; configuración administrativa y recurso `usuarios.js?v=4` cargaron correctamente.
[2026-08-22 18:50] · Módulo: Dashboard de usuarios · Cambio: Se corrigen en `usuarios.php` el acceso a la contraseña opcional ausente durante edición y la serialización de nombres de usuario en handlers inline de suspender/reactivar, evitando avisos y ruptura de JavaScript con datos especiales. · Archivos: `dashboard/usuarios.php`, `docs/BITACORA.md` · Prueba: `php -l dashboard/usuarios.php`; recarga autenticada sin errores de consola ni respuestas fallidas; handler de `qa_usuario` generado como `confirmarToggle(7, "qa_usuario", 'suspender')`; no se modificaron cuentas.
[2026-08-22 19:10] · Módulo: Dashboard de perfil · Cambio: Se corrige en `perfil.js` el observer del sidebar para no ejecutar `classList` sobre `mainWrapper` nulo; se versiona el recurso cargado por `perfil.php`. · Archivos: `assets/dashboard/perfil.js`, `dashboard/perfil.php`, `docs/BITACORA.md` · Prueba: `node --check assets/dashboard/perfil.js`; `php -l dashboard/perfil.php`; auditoría autenticada de `dashboard/index.php`, `dashboard/usuarios.php` y `dashboard/perfil.php` sin errores de consola ni respuestas `4xx/5xx`.
[2026-08-22 19:25] · Módulo: Dashboard de perfil · Cambio: Se corrige la consulta inicial de `perfil.php` para incluir `pregunta_seguridad`; antes el selector siempre mostraba “Seleccione una pregunta...” aunque el usuario tuviera una pregunta guardada. · Archivos: `dashboard/perfil.php`, `docs/BITACORA.md` · Prueba: `php -l dashboard/perfil.php`; navegador autenticado mostró `Nombre de tu mascota` seleccionado, sin errores de consola ni respuestas fallidas y sin overflow móvil.
[2026-08-22 19:40] · Módulo: Validación de perfil · Cambio: Se valida el rechazo de cambios de perfil cuando la contraseña actual es incorrecta. · Archivos: `docs/BITACORA.md` · Prueba: el formulario mostró `Debes escribir tu contraseña actual para guardar los cambios.` y la consulta en `jv3000_db_test` confirmó que la cuenta Administrador conserva usuario, correo, estado y aprobación; no se modificó la base real.
[2026-08-22 20:15] · Módulo: Recuperación de contraseña · Cambio: Se corrige el reinicio prematuro del flujo cuando `recuperar.php?reset=1` recibía formularios POST; el parámetro ahora solo limpia el estado en GET, permitiendo avanzar de búsqueda a pregunta y cambio de contraseña. · Archivos: `login/recuperar.php`, `docs/BITACORA.md` · Prueba: fallo reproducido al responder la pregunta, que devolvía `SOLICITUD INVÁLIDA`; validación posterior del flujo completo pendiente sobre `jv3000_db_test`.
[2026-08-22 20:35] · Módulo: Continuidad de pruebas · Cambio: Se completa la recuperación de contraseña de `qa_usuario` mediante usuario, pregunta de seguridad y nueva clave; luego se restaura la clave original de QA para conservar el estado conocido del entorno. · Archivos: `login/recuperar.php`, `docs/BITACORA.md` · Prueba: el flujo pasó por los pasos 1, 2, 3 y 4, mostró `CONTRASEÑA ACTUALIZADA` y el inicio de sesión posterior redirigió al Dashboard con rol `Operador de Ventas`; no se modificó la base real.
[2026-08-22 20:50] · Módulo: Auditoría de permisos · Cambio: Se verifica el acceso del usuario QA aprobado con rol 3 frente a rutas permitidas y restringidas. · Archivos: `docs/BITACORA.md` · Prueba: `qa_usuario` accedió a Ventas, Estadísticas y Perfil; Compras, Recepción, Categorías y Proveedores devolvieron `acceso_denegado`; Usuarios e Historial devolvieron `acceso_prohibido`; la navegación del sidebar coincide con esos permisos.
[2026-08-22 21:10] · Módulo: Permisos y navegación · Cambio: Se detecta que el backend permite Categorías y Proveedores al rol 2 mediante `verificarPermisoCarga()`, pero el sidebar los ocultaba al Operador de Carga. Se ajusta la navegación para mostrar ambos módulos a roles 1 y 2, manteniendo sus controles administrativos donde corresponda. · Archivos: `includes/sidebar.php`, `docs/BITACORA.md` · Prueba: `qa_carga` (rol 2) accedió correctamente a Inventario, Compras, Recepción, Solicitudes, Perfil y Proveedores; Ventas, Estadísticas, Usuarios e Historial fueron restringidos; validación final del menú pendiente.
[2026-08-22 21:25] · Módulo: Auditoría de permisos · Cambio: Se completa la validación del rol 2 con la cuenta QA `qa_carga`, comprobando que la navegación visible coincide con sus permisos efectivos. · Archivos: `includes/sidebar.php`, `docs/BITACORA.md` · Prueba: el sidebar mostró Inventario, Compras, Recepción, Solicitudes, Proveedores, Categorías y Perfil; ocultó Ventas, Estadísticas, Usuarios e Historial; las rutas de carga permitidas respondieron correctamente y las restringidas redirigieron; no se modificó la base real.
[2026-08-22 22:00] · Módulo: Cierre de validación · Cambio: Se consolidan las verificaciones de AJAX autenticado, CSRF, integridad de datos, accesibilidad y automatización del proyecto. · Archivos: `tools/validar.ps1`, `.htaccess`, `dashboard/usuarios.php`, `dashboard/perfil.php`, `docs/BITACORA.md` · Prueba: Estadísticas AJAX autenticado respondió `200` con JSON válido; CSRF válido llegó al controlador y token ausente respondió `403`; integridad en `jv3000_db_test` devolvió 0 fallos en stock/lotes, vínculos, huérfanos y movimientos anulados; Usuarios y Perfil quedaron sin overflow ni controles sin etiqueta; `tools/validar.ps1` pasó sintaxis, bloqueos HTTP e integridad.
[2026-08-22 22:05] · Módulo: Preparación para despliegue · Cambio: Se audita configuración y permisos sin modificarlos automáticamente: la aplicación apunta a `jv3000_db_test`, el backup apunta a `jv3000_db`, no existe `.env` y `Authenticated Users` tiene `Modify` sobre directorios internos. · Archivos: `includes/config.php`, `backups/backup.bat`, `tools/validar.ps1`, `docs/BITACORA.md` · Prueba: `mysqldump.exe` existe y `.htaccess` bloquea rutas internas; quedan como acciones manuales separar credenciales, alinear el destino del backup y restringir ACL antes de producción.
[2026-08-22 22:30] · Módulo: Automatización de validación · Cambio: Se amplía `tools/validar.ps1` para informar configuración de despliegue: base de la aplicación, base del backup, existencia de `.env` y disponibilidad de `mysqldump`; emite una advertencia si los destinos no coinciden, sin bloquear las pruebas ni cambiar configuración. · Archivos: `tools/validar.ps1`, `docs/BITACORA.md` · Prueba: el validador pasó sintaxis, bloqueo HTTP e integridad; reportó aplicación `jv3000_db_test`, backup `jv3000_db`, `.env=False` y `mysqldump=True`, con advertencia de discrepancia; no se modificaron credenciales ni bases de datos.
[2026-08-22 22:45] · Módulo: Configuración de despliegue · Cambio: Se habilita configuración externa mediante `JV_DB_HOST`, `JV_DB_USER`, `JV_DB_PASS` y `JV_DB_NAME`, se añade `config/.env.example` sin secretos y el script de backup usa las mismas variables con fallback conservador. El entorno actual conserva `jv3000_db_test` si no se definen variables. · Archivos: `config/.env.example`, `includes/config.php`, `backups/backup.bat`, `docs/BITACORA.md` · Prueba: `php -l includes/config.php`; no se creó `.env` real ni se conectó o modificó la base real.
[2026-08-22 23:00] · Módulo: Automatización de validación · Cambio: Se corrige `tools/validar.ps1` para interpretar la configuración efectiva basada en `JV_DB_NAME`, tanto en PHP como en el backup, evitando reportes falsos al ejecutar con variables de entorno. · Archivos: `tools/validar.ps1`, `docs/BITACORA.md` · Prueba: ejecución con `JV_DB_NAME=jv3000_db_test` pasó sin discrepancia, con sintaxis, HTTP e integridad en verde; no se modificaron datos.
[2026-08-22 23:15] · Módulo: Automatización de validación · Cambio: Se corrige el reconocimiento del fallback de `DB_NAME` en `tools/validar.ps1` para que la ejecución sin variables de entorno informe correctamente `jv3000_db_test` y no muestre una discrepancia falsa. · Archivos: `tools/validar.ps1`, `docs/BITACORA.md` · Prueba: ejecución por defecto informó aplicación `jv3000_db_test`, backup `jv3000_db`, `.env=False`, `mysqldump=True`, mantuvo la advertencia real de discrepancia y terminó con validación OK; no se modificaron datos.
[2026-08-22 23:30] · Módulo: Calidad de repositorio · Cambio: Se añade `.gitattributes` para definir finales de línea consistentes: LF en código/documentación y CRLF en scripts `.bat`, evitando ambigüedad futura entre Windows y Git. · Archivos: `.gitattributes`, `docs/BITACORA.md` · Prueba: `tools/validar.ps1` pasó completamente; Git solo reporta avisos de normalización de cuatro archivos existentes, sin errores de contenido.
[2026-08-22 23:45] · Módulo: Organización de configuración · Cambio: Se mueve `.env.example` a `config/.env.example` para mantener los archivos de configuración juntos. `.git/` y `.htaccess` permanecen en la raíz porque Git y Apache requieren esas ubicaciones; no existe un `.env` real. · Archivos: `config/.env.example`, `tools/validar.ps1`, `docs/BITACORA.md` · Prueba: verificación de estructura y validador actualizado para buscar `config/.env`; no se modificaron credenciales ni bases de datos.
[2026-08-23 00:20] · Módulo: Configuración productiva · Cambio: Tras verificar la restauración aislada del backup y eliminar la base temporal, se crea `config/.env` ignorado por Git con `JV_DB_NAME=jv3000_db`; PHP y `backup.bat` quedan configurados para leer las mismas variables. · Archivos: `config/.env`, `includes/config.php`, `backups/backup.bat`, `docs/BITACORA.md` · Prueba: backup más reciente restaurado previamente en base temporal con 20 tablas y 21 FKs; base temporal eliminada; no se ejecutaron escrituras sobre `jv3000_db`.
[2026-08-23 00:50] · Módulo: Permisos de despliegue · Cambio: Se inicia el endurecimiento de ACL solicitado, retirando `Authenticated Users` de `backups` y `db`; la operación masiva para las seis carpetas restantes fue omitida por el entorno y queda pendiente. · Archivos: `backups`, `db`, `docs/BITACORA.md` · Prueba: validador confirmó `backups` y `db` sin escritura amplia; `models`, `controllers`, `views`, `config`, `includes` y `core` aún muestran advertencia; no se modificaron bases de datos.
[2026-08-23 01:00] · Módulo: Permisos de despliegue · Cambio: Se intenta continuar con `models`, pero Windows devuelve `Acceso denegado` al deshabilitar herencia y retirar `Authenticated Users`; no se alteró esa carpeta. · Archivos: `models`, `docs/BITACORA.md` · Prueba: `icacls models /inheritance:d` y `/remove:g` rechazados sin elevación; se requiere una consola de Administrador para continuar con `models`, `controllers`, `views`, `config`, `includes` y `core`.
[2026-08-23 01:20] · Módulo: Permisos de despliegue · Cambio: Se completa el endurecimiento ACL de las ocho carpetas internas retirando la escritura amplia de `Authenticated Users` y conservando permisos de Administradores, SYSTEM y lectura/ejecución para Usuarios. · Archivos: `backups`, `db`, `models`, `controllers`, `views`, `config`, `includes`, `core`, `docs/BITACORA.md` · Prueba: `tools/validar.ps1` confirmó `[OK] ACL` para las ocho carpetas; sintaxis, rutas HTTP e integridad también terminaron correctamente.
[2026-08-22 02:00] · Módulo: Documentación de despliegue · Cambio: Se actualiza el plan para reflejar que las pruebas funcionales, seguridad, accesibilidad, automatización y preparación productiva ya fueron completadas; se mantiene la prohibición de escrituras operativas no respaldadas sobre `jv3000_db`. · Archivos: `docs/PLAN DE CONTINUACION.md`, `docs/BITACORA.md` · Prueba: `tools/validar.ps1` terminó correctamente con sintaxis, rutas HTTP, integridad, configuración y ACL en verde.
[2026-08-22 14:30] · Módulo: Backup y restauración · Cambio: Se corrige el script para usar opciones de consistencia y detectar errores reales de `mysqldump`; se genera un respaldo temporal de `jv3000_db` y se verifica su restauración aislada. · Archivos: `backups/backup.bat`, `docs/BITACORA.md` · Prueba: respaldo temporal válido de 38922 bytes; la primera tentativa no fue una restauración aislada válida porque el dump contiene `USE jv3000_db`; se verificó la base real y luego se repitió correctamente con filtrado controlado hacia una base temporal, obteniendo 20 tablas, 21 claves foráneas y 3 usuarios; la base temporal fue eliminada.
[2026-08-22 15:00] · Módulo: Dashboard · Cambio: Se tipa el parámetro `$db` de `obtenerDatosDashboard` como `Database` para eliminar el diagnóstico de tipo desconocido y reflejar la instancia singleton utilizada por la función. · Archivos: `dashboard/index.php`, `docs/BITACORA.md` · Prueba: `php -l dashboard/index.php` y `tools/validar.ps1` sin errores.
[2026-08-22 15:30] · Módulo: Dashboard · Cambio: Se corrige la valoración del inventario para calcular el costo de las existencias mediante los lotes restantes, manteniendo fallback para productos históricos sin lotes, y se aclara que las últimas compras muestran el total de factura con IVA incluido. · Archivos: `dashboard/index.php`, `docs/BITACORA.md` · Prueba: consulta de solo lectura comparó compras recibidas, stock y lotes; `tools/validar.ps1` terminó correctamente.
[2026-08-22 16:00] · Módulo: Documentación · Cambio: Se elimina `PLAN DE CONTINUACION.md` porque las tareas previstas ya fueron completadas y el plan dejó de ser necesario como documento activo. · Archivos: `docs/PLAN DE CONTINUACION.md`, `docs/BITACORA.md` · Prueba: estado del repositorio revisado antes del commit; el validador completo se ejecuta después del cambio.
[2026-08-22 16:30] · Módulo: Inventario · Cambio: Se corrige el desborde horizontal del módulo en pantallas pequeñas y se reduce el trabajo del tooltip global eliminando el reposicionamiento en cada movimiento del mouse. · Archivos: `assets/modules/productos/productos.css`, `assets/js/tooltips.js`, `docs/BITACORA.md` · Prueba: `node --check assets/js/tooltips.js` y `tools/validar.ps1` terminaron correctamente.
[2026-08-22 17:00] · Módulo: Inventario · Cambio: Se ajusta también la composición para PC y laptops, limitando el contenido al ancho disponible, compactando la tabla en portátiles y actualizando la versión del CSS para evitar caché antiguo. · Archivos: `assets/modules/productos/productos.css`, `controllers/ProductosController.php`, `docs/BITACORA.md` · Prueba: `tools/validar.ps1` terminó correctamente.
[2026-08-22 17:30] · Módulo: Inventario · Cambio: Se corrige la densidad visual de la tabla para evitar que badges, números, bordes y textos choquen; se habilita salto controlado en la celda de stock, se reducen espacios internos y se estabiliza el modelo de tabla para PC, laptop y móvil. · Archivos: `assets/modules/productos/productos.css`, `controllers/ProductosController.php`, `docs/BITACORA.md` · Prueba: `tools/validar.ps1` terminó correctamente.
[2026-08-22 18:00] · Módulo: Inventario · Cambio: Se corrige el badge de vencimiento para mantener icono y fecha dentro de la celda con elipsis controlado, y se sustituye la papelera del botón de desactivar por `toggle-off`, que representa el cambio de estado. · Archivos: `views/productos/index.php`, `assets/modules/productos/productos.css`, `controllers/ProductosController.php`, `docs/BITACORA.md` · Prueba: `php -l views/productos/index.php` y `tools/validar.ps1` terminaron correctamente.
[2026-08-22 18:30] · Módulo: Inventario · Cambio: Se diseña el modal de edición con ancho amplio, cabecera teal, fondo de formulario, campos espaciados y controles visualmente consistentes para PC, laptop y móvil. · Archivos: `views/productos/index.php`, `assets/modules/productos/productos.css`, `controllers/ProductosController.php`, `docs/BITACORA.md` · Prueba: `php -l views/productos/index.php` y `tools/validar.ps1` terminaron correctamente.
[2026-08-22 19:00] · Módulo: Inventario · Cambio: Se reemplaza el icono `toggle-off` del botón de desactivar por `power` para distinguir con mayor claridad la acción de cambio de estado frente a editar y reactivar. · Archivos: `views/productos/index.php`, `docs/BITACORA.md` · Prueba: `php -l views/productos/index.php` y `tools/validar.ps1` terminaron correctamente.
[2026-08-22 19:30] · Módulo: Inventario · Cambio: Se retira de la interfaz la explicación técnica `(0 = categoría)` conservando la herencia interna, y se alinea la validación de stocks y precios: stock mínimo positivo, capacidad máxima cero o positiva con relación válida al mínimo, precios mayores que cero y venta no menor que costo. · Archivos: `views/productos/index.php`, `assets/modules/productos/productos.js`, `models/Producto.php`, `controllers/ProductosController.php`, `docs/BITACORA.md` · Prueba: `php -l` de vista y modelo, `node --check` del JavaScript y `tools/validar.ps1` terminaron correctamente; diagnósticos del editor sin errores.
[2026-08-22 20:00] · Módulo: Inventario · Cambio: Se establece formato automático de dos decimales para `PRECIO VENTA` y `PRECIO COSTO` al salir de los campos y antes de guardar, manteniendo entradas numéricas y las validaciones existentes. · Archivos: `assets/modules/productos/productos.js`, `controllers/ProductosController.php`, `docs/BITACORA.md` · Prueba: `php -l` de vista y controlador, `node --check` del JavaScript, diagnósticos del editor y `tools/validar.ps1` terminaron correctamente.
[2026-08-22 20:30] · Módulo: Repositorio · Cambio: Se ajusta `.gitattributes` para declarar explícitamente los archivos que conservan finales CRLF y se configura `core.autocrlf=false` localmente, eliminando avisos de conversión sin modificar el contenido lógico del proyecto. · Archivos: `.gitattributes`, `docs/BITACORA.md` · Prueba: `git diff --check`, `git ls-files --eol` y `tools/validar.ps1` terminaron correctamente.
[2026-08-22 21:00] · Módulo: Inventario · Cambio: Se tipan como `Database` los parámetros `$db` de los cinco helpers FEFO para eliminar el diagnóstico de tipo desconocido y documentar el contrato real de la conexión. · Archivos: `includes/helpers.php`, `docs/BITACORA.md` · Prueba: `php -l includes/helpers.php`, `tools/validar.ps1` y diagnósticos del editor terminaron correctamente.
[2026-08-22 21:30] · Módulo: Inicialización · Cambio: Se tipan los parámetros de `jv_importar_sql` como `mysqli` y `string`, eliminando el diagnóstico de tipo desconocido para `$sql_path` y documentando el contrato real de la importación SQL. · Archivos: `init.php`, `docs/BITACORA.md` · Prueba: `php -l init.php`, `tools/validar.ps1` y diagnósticos del editor terminaron correctamente.
[2026-08-22 22:00] · Módulo: Inventario · Cambio: Se añade el tooltip global al badge de vencimiento para mostrar la fecha completa en tamaño grande al pasar el mouse, incluyendo un texto claro cuando el producto no tiene fecha. · Archivos: `views/productos/index.php`, `docs/BITACORA.md` · Prueba: `php -l views/productos/index.php`, `tools/validar.ps1` y diagnósticos del editor terminaron correctamente.
[2026-08-22 22:30] · Módulo: Inventario · Cambio: Se simplifica el indicador de stock eliminando el mensaje de estado y dejando solo el número, una barra dinámica por nivel y la referencia de mínimo y máximo. · Archivos: `views/productos/index.php`, `assets/modules/productos/productos.css`, `controllers/ProductosController.php`, `docs/BITACORA.md` · Prueba: `php -l`, `node --check` y `tools/validar.ps1` terminaron correctamente; diagnósticos del editor sin errores.
[2026-08-22 23:00] · Módulo: Inventario · Cambio: Se formatean automáticamente los precios de edición con separador local de miles y dos decimales (`100,00`, `1.000,00`, `100.000,00`), se limita el valor máximo y se normaliza también en el controlador para aceptar entradas manuales de forma segura. · Archivos: `views/productos/index.php`, `assets/modules/productos/productos.js`, `controllers/ProductosController.php`, `docs/BITACORA.md` · Prueba: `php -l`, `node --check` y `tools/validar.ps1` terminaron correctamente.
[2026-08-22 23:15] · Módulo: Inventario · Cambio: Se refuerza el límite de precios en el modelo para rechazar valores no finitos y cifras superiores a `999.999,00`, con mensajes específicos también en la validación del navegador. · Archivos: `assets/modules/productos/productos.js`, `models/Producto.php`, `docs/BITACORA.md` · Prueba: `php -l`, `node --check` y `tools/validar.ps1` terminaron correctamente.
[2026-08-22 23:30] · Módulo: Inventario · Cambio: Se aplica el formateo también durante la escritura, conservando temporalmente separadores decimales incompletos para no interrumpir la captura. · Archivos: `assets/modules/productos/productos.js`, `docs/BITACORA.md` · Prueba: `node --check` y `tools/validar.ps1` terminaron correctamente.
[2026-08-22 23:45] · Módulo: Inventario · Cambio: Se corrige la captura de precios para eliminar letras sin bloquear el borrado, conservar el cursor y evitar el reformateo a golpes durante cada tecla; el formato de miles y dos decimales se aplica al salir del campo y los valores fuera de rango ya no se recortan silenciosamente. · Archivos: `assets/modules/productos/productos.js`, `controllers/ProductosController.php`, `docs/BITACORA.md` · Prueba: `node --check`, `php -l`, `tools/validar.ps1` y diagnósticos del editor terminaron correctamente.
[2026-08-22 23:55] · Módulo: Inventario · Cambio: Se ajusta el formateo en vivo para agrupar miles sin saltos del cursor, aceptar coma o punto decimal, eliminar letras y permitir borrar el campo sin reformatearlo prematuramente; se actualiza la versión del JavaScript a `v=9`. · Archivos: `assets/modules/productos/productos.js`, `controllers/ProductosController.php`, `docs/BITACORA.md` · Prueba: validación pendiente de ejecución.
[2026-08-23 00:10] · Módulo: Inventario · Cambio: Se integra la regla estricta de precio técnico `^(?:0|[1-9]\d{0,4})\.\d{2}$` con límite `99.999,99`, rechazando letras, negativos, `Infinity`, `NaN`, más de dos decimales y cifras fuera de rango; la interfaz conserva formato local con miles y coma decimal. · Archivos: `views/productos/index.php`, `assets/modules/productos/productos.js`, `controllers/ProductosController.php`, `models/Producto.php`, `docs/BITACORA.md` · Prueba: `php -l`, `node --check`, `tools/validar.ps1` y diagnósticos del editor terminaron correctamente.
[2026-08-23 00:20] · Módulo: Inventario · Cambio: Se muestra inmediatamente el formato local de dos decimales al abrir el modal de edición (`1.000,00`) y se actualiza la versión del JavaScript para evitar caché del navegador. · Archivos: `assets/modules/productos/productos.js`, `controllers/ProductosController.php`, `docs/BITACORA.md` · Prueba: `node --check`, `php -l`, `tools/validar.ps1`, diagnósticos del editor y comprobación HTTP del recurso `v=10` terminaron correctamente.
[2026-08-23 00:35] · Módulo: Inventario · Cambio: Se separa el valor visual localizado del valor técnico enviado, usando campos ocultos canónicos para evitar que comas y puntos interfieran con el guardado; se fija el mínimo en `0,01` y se mantiene el máximo en `99.999,99`. · Archivos: `views/productos/index.php`, `assets/modules/productos/productos.js`, `models/Producto.php`, `controllers/ProductosController.php`, `docs/BITACORA.md` · Prueba: validación pendiente de ejecución.
[2026-08-22 16:30] · Módulo: Navegación · Cambio: Se corrige el estado activo del sidebar para que el Panel de Inicio no permanezca seleccionado en las rutas MVC; ahora solo se marca cuando el `index.php` no tiene una ruta `url`. · Archivos: `includes/sidebar.php`, `docs/BITACORA.md` · Prueba: revisión de las rutas MVC y validación completa ejecutada después del cambio.
[2026-08-23 00:35] · Módulo: Automatización de validación · Cambio: Se actualiza `tools/validar.ps1` para leer `config/.env` y reportar la configuración efectiva productiva cuando no existen variables de entorno del proceso. · Archivos: `tools/validar.ps1`, `docs/BITACORA.md` · Prueba: ejecución pendiente; no se modificaron datos.
[2026-08-23 00:00] · Módulo: Automatización de despliegue · Cambio: Se añade al validador un informe no destructivo de ACL para las carpetas internas, mostrando advertencias cuando existen permisos de escritura amplios sin modificar el sistema de archivos. · Archivos: `tools/validar.ps1`, `docs/BITACORA.md` · Prueba: ejecución posterior reportó advertencias en `backups`, `db`, `models`, `controllers`, `views`, `config`, `includes` y `core`; sintaxis, HTTP e integridad terminaron en `[OK]`; no se cambiaron permisos ni datos.
[2026-08-22 22:15] · Módulo: Calidad de código · Cambio: Se eliminan cuatro espacios finales detectados por `git diff --check` en `dashboard/usuarios.php`; no se modifica la lógica ni el comportamiento de la pantalla. · Archivos: `dashboard/usuarios.php`, `docs/BITACORA.md` · Prueba: `git diff --check` sin errores de espacios; lint PHP conservado.
[2026-08-22 22:00] · Módulo: Cierre de validación y preparación para despliegue · Cambio: Se ejecutan los seis bloques pendientes: AJAX autenticado de Estadísticas, CSRF autenticado, integridad de base, accesibilidad responsive, revisión de configuración/backup y automatización. Se crea `tools/validar.ps1` para repetir lint PHP/JS, bloqueo HTTP e integridad sin credenciales en el archivo. · Archivos: `tools/validar.ps1`, `dashboard/usuarios.php`, `dashboard/perfil.php`, `assets/dashboard/usuarios.css`, `docs/BITACORA.md` · Prueba: Estadísticas AJAX `HTTP 200` con JSON válido; CSRF válido con datos vacíos `400` por validación de negocio y CSRF ausente `403`; integridad en cero para stock/lotes, vínculos, huérfanos y movimientos anulados; Usuarios y Perfil sin overflow ni controles sin etiqueta, Compras/Ventas sin overflow pero con controles sin etiqueta pendientes; validador completo `[OK]`; `.htaccess` bloquea rutas internas en `403`; revisión de despliegue detectó `DB_NAME=jv3000_db_test` frente a backup `jv3000_db`, credenciales en código y permisos Modify amplios, pendientes de decisión antes de producción. La base real no fue modificada.
[2026-08-22 20:00] · Módulo: Permisos y navegación · Cambio: Se corrige la inconsistencia por la que el sidebar mostraba Inventario al rol 3 aunque el backend restringe `productos` a roles 1 y 2; ahora el menú refleja el permiso efectivo y evita enlaces que terminan en acceso denegado. · Archivos: `includes/sidebar.php`, `docs/BITACORA.md` · Prueba: con `qa_usuario` (rol 3), backend rechazó `productos` y la navegación fue ajustada para no mostrar esa opción; no se modificaron datos.
[2026-08-22 19:40] · Módulo: Recursos del dashboard · Cambio: Se eliminan las cargas duplicadas de SweetAlert2 en `usuarios.php` y `perfil.php`; `sidebar.php` ya proporciona una única carga compartida. Se conserva Bootstrap, necesario para los modales de cada página. · Archivos: `dashboard/usuarios.php`, `dashboard/perfil.php`, `docs/BITACORA.md` · Prueba: `php -l` sin errores en ambas páginas; navegador autenticado confirmó una referencia a SweetAlert2 y una a Bootstrap por página, sin errores de consola ni respuestas fallidas.
[2026-08-22 17:50] · Módulo: Usuarios · Cambio: Se incorpora al dashboard una acción explícita para aprobar cuentas pendientes y asignar un rol operativo 2 o 3; se valida el estado pendiente, se activa la cuenta y se registra la auditoría. Esto corrige la limitación por la que un usuario nuevo solo podía reactivarse y quedaba sin rol. · Archivos: `dashboard/usuarios.php`, `assets/dashboard/usuarios.css`, `docs/BITACORA.md` · Prueba: PHP y revisión funcional del formulario de aprobación pendientes; no se modificó ninguna cuenta existente.
[2026-08-22 18:05] · Módulo: Continuidad de pruebas · Cambio: Se completa el flujo de registro y aprobación de usuarios en `jv3000_db_test`: la cuenta QA se registró pendiente, se aprobó desde el dashboard y recibió el rol Operador de Ventas. · Archivos: `dashboard/usuarios.php`, `assets/dashboard/usuarios.css`, `docs/BITACORA.md` · Prueba: la interfaz pasó de `Pendientes 1 / Activos 3` a `Pendientes 0 / Activos 4`; SQL confirmó `qa_usuario` con `id_rol=3`, `status=Activo` y `aprobado=1`; no se modificó la base real.

[2026-08-23 19:22] · Módulo: Documentación del agente · Cambio: Se unifican las reglas del agente en una sola fuente: las convenciones útiles de `docs/Instrucciones_Agente_IA.md` (modo de trabajo directo/quirúrgico, separación Vista-JS-Backend, flujo AJAX) se integran a `AGENTS.md` como nueva sección "Convenciones de código", corregidas para reflejar el estándar real verificado en el código (funciones JS camelCase sin prefijos, IDs descriptivos sin prefijos cmb/txt/btn — 0 usos detectados, variables PHP snake_case, controladores MVC `[Modulo]Controller.php`, solo fetch nativo sin jQuery). El archivo original quedó obsoleto (nunca commiteado, previo a la migración MVC) y se elimina. No se modifica código. · Archivos: `AGENTS.md`, `docs/Instrucciones_Agente_IA.md` (eliminado), `docs/BITACORA.md` · Prueba: verificación con búsqueda de prefijos (`cmb_`, `txt_`, `btn_`, `fn_` = 0 coincidencias) y muestra de funciones reales (`abrirRecepcion`, IDs `edit_pvp`, `formEditar`) antes de redactar las reglas.

[2026-08-23 19:46] · Módulo: Documentación · Cambio: Se elimina definitivamente `docs/GUIA DE PROYECTO.md` del repositorio (borrado previo en el working tree, ahora commiteado). Su contenido normativo quedó cubierto por `AGENTS.md` y la sección "Convenciones de código". No se modifica código ni datos. · Archivos: `docs/GUIA DE PROYECTO.md` (eliminado), `docs/BITACORA.md` · Prueba: `Test-Path docs/GUIA DE PROYECTO.md` = False; `git status` confirma la eliminación registrada.

[2026-08-23 22:16] · Módulo: Validaciones pendientes (precios) · Cambio: Se ejecutan las validaciones que quedaron pendientes en las entradas de 2026-08-23 00:35 y 00:20 del flujo de precios con campos canónicos. Verificación completa sin errores; no se modificó ningún archivo. · Archivos: ninguno (solo verificación) · Prueba: `php -l views/productos/index.php`, `php -l models/Producto.php`, `php -l controllers/ProductosController.php`, `node --check assets/modules/productos/productos.js` = OK; `tools/validar.ps1` completo `[OK]`; smoke HTTP con login real CSRF de `?url=productos` = 200 renderizando `formEditar`, `edit_pvp_valor` y `edit_costo_valor`.

[2026-08-23 22:16] · Módulo: Feedback de usuario (Proveedores, Sidebar, Ventas) · Cambio: Mejoras derivadas de revisión externa. (1) Proveedores — errores por campo: texto de ayuda visible bajo el RIF ("Formato: J-12345678-0"), atributos nativos `pattern`/`title` en el RIF para bloqueo previo por el navegador, y el flash de error del servidor ahora viaja en `data-texto` (`#flashMsg`) para que el JS marque en rojo el campo exacto que lo causó vía `marcarError()` (mapeo RIF/NOMBRE/CORREO/TELÉFONO). (2) Sidebar fijo en escritorio: media query `min-width:769px` fuerza el sidebar siempre abierto con margen fijo 260px, oculta el botón toggle y neutraliza el backdrop; el colapso queda reservado al móvil. (3) CSS Proveedores amontonado: los botones de acción de la cabecera de tarjeta estaban como hijos sueltos del flex `space-between` y quedaban dispersos; se agrupan en un contenedor propio a la derecha y `.prov-head` gana `gap:10px;flex-wrap:wrap`. (4) Ventas — RIF/cédula: formato unificado sin puntos (guiones, igual que Proveedores) y cédula V/E exigida ahora en 9 dígitos fijos (antes aceptaba desde 6); el valor oculto enviado al backend no cambia de formato. (5) Accesibilidad: 34 controles sin etiqueta en Compras (20) y Salidas (14) reciben `aria-label` descriptivo, incluyendo las 2 fechas deshabilitadas de Compras. · Archivos: `views/proveedores/index.php`, `assets/modules/proveedores/proveedores.js`, `assets/modules/proveedores/proveedores.css`, `assets/css/sidebar.css`, `assets/modules/salidas/salidas.js`, `views/compras/index.php`, `views/salidas/index.php`, `docs/BITACORA.md` · Prueba: `php -l` sin errores en proveedores/compras/salidas; `node --check` OK en proveedores.js y salidas.js; re-verificación de etiquetas = 0 controles sin `aria-label`; `tools/validar.ps1` completo `[OK]`; smoke HTTP con login real: `?url=proveedores` = 200 con hint de RIF, `?url=compras` = 200 con 20 aria-label, `?url=salidas` = 200 con 14 aria-label, `?url=productos` = 200.

---

## Plantilla para futuras entradas

Copiar la línea de abajo al final de la sección Registro, completar y borrar esta plantilla cuando ya exista al menos una entrada real.

```
[YYYY-MM-DD HH:MM] · Módulo: · Cambio: · Archivos: · Prueba:
```
